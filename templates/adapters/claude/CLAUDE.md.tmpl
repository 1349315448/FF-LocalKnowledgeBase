# Claude Code Project Instructions

Use the FF Skills installed for this workspace. Before implementation, query project knowledge with:

`ffkb query --root "{{KNOWLEDGE_ROOT}}" --intent "<request>"`

Canonical standards and project facts live under `{{KNOWLEDGE_ROOT}}`. Load only the relevant compiled truth and keep this file short.
