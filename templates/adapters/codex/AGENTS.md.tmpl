# Codex Project Instructions

Use the FF Skills installed for this workspace. Start development work with `ff-develop` and query:

`ffkb query --root "{{KNOWLEDGE_ROOT}}" --intent "<request>"`

Keep this file as a thin entrypoint. Canonical standards and project facts live under `{{KNOWLEDGE_ROOT}}`; do not copy them here.
